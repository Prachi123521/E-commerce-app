{/*import React from 'react';
import Data from '../data';

const Shopping = () => {
  return (
    <div>
    {Data.map((Data)=>(
        key={Data.images}
    ))}
    
    </div>
  )
}

export default Shopping;
*/}